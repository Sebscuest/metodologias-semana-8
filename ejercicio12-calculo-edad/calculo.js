'use strict';

/*
Escribir una funcion que reciba como argumento la edad de una persona y devuelva true si es mayor a 18
y false si es menor 
*/ 

/*function calculoEdad(edad){
       if (edad > 18) {
            return true;
        } else {
            return false;
        }
        return (edad > 18);
    }
let edad = Number(prompt('Ingrese su edad'));
let resultado = calculoEdad(edad);
alert('El resultado es ' + resultado); */

/*
Escribir una funcion que miltiplique dos numeros y devuelva el resultado .
N: usar solo la operacion + 
*/
let multiplicardor = 3;
let multiplicando = 2;
let suma=0;
for (let i=0; i< multiplicardor; i++){
    suma =suma + multiplicando;
}
alert (suma);