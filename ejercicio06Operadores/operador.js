'use strict'

 let a = Number (prompt('Ingrese el valor de a'));  //declaramos las variables y asigno 1 
 let b = Number (prompt('Ingrese el valor de b'));
 let result;
 result = a+b;
 //Almacenar en la variable rsult la suma de a y b.
 //Luego mostrar el contenido de result
 alert(result);
 //Multiplicar los valores de a y b y almacenar en result
 result = a*b;
 alert(result);
 //Hallar el resto de a para b 
result = a%b;
alert(result);
//Crear una variable a2 y asignar el valor de a en Negativo
//Mostrar el resultado de a2
let a2= -a;

//Incrementar el valor de a en uno
a++;// a=a+1
alert(a);
//Decrementar el valor de b en uno
b--; //b=b-1
alert(b);
